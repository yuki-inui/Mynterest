const PrimeNumber = (num) => {
  if(num === 2) {
    console.log(2);
  }else{
    for(let i = 2; i < N; i++){
      if(N % i === 0) {
        break;
      }else {
        console.log(i);
      }
    }
  }
  
};

let N = 191
